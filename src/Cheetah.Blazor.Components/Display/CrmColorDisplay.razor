@namespace Cheetah.Blazor.Components.Display

@if (!string.IsNullOrEmpty(Color))
{
    <span class="crm-color-display" style="background-color: @Color;" title="@Color"></span>
}
else
{
    <span>—</span>
}

@code {
    [Parameter] public string? Color { get; set; }
}
