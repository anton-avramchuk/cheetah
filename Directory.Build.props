<Project>

    <PropertyGroup>
        <!-- Общие свойства для всех проектов -->
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>

        <!-- Версия пакетов Microsoft -->
        <MsPackageVersion>10.0.*</MsPackageVersion>
    </PropertyGroup>

</Project>
